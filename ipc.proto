syntax = "proto3";

package ipc;
option go_package = "./ipc";

message WsnSensorDataTelemetry {
    uint32 timestamp = 1;
    string sensor_id = 2;
    float temperature = 3; 
    float humidity = 4;
}
